apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ app_name }}-netpol
  namespace: {{ namespace }}
  labels:
    app: {{ app_name }}
    managed-by: kubeforge
spec:
  podSelector:
    matchLabels:
      app: {{ app_name }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: {{ port }}
  egress:
    - {}
