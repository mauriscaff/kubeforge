apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}-svc
  namespace: {{ namespace }}
  labels:
    app: {{ app_name }}
    managed-by: kubeforge
spec:
  type: {{ service_type }}
  selector:
    app: {{ app_name }}
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: {{ port }}
