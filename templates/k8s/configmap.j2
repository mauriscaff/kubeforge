apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ app_name }}-config
  namespace: {{ namespace }}
  labels:
    app: {{ app_name }}
    managed-by: kubeforge
data:
{% for key, value in config_vars.items() %}
  {{ key }}: "{{ value }}"
{% endfor %}
