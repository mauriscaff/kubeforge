apiVersion: v1
kind: Secret
metadata:
  name: {{ app_name }}-secret
  namespace: {{ namespace }}
  labels:
    app: {{ app_name }}
    managed-by: kubeforge
type: Opaque
stringData:
{% for key in secret_vars %}
  {{ key }}: "REPLACE_ME"
{% endfor %}
